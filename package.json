{
  "devDependencies": {
    "markdownlint-cli": "0.27.1"
  },
  "scripts": {
    "lint": "markdownlint --config .markdownlint.json --ignore-path wiki/.markdownlintignore wiki/*.md",
    "test": "npm run lint"
  }
}
